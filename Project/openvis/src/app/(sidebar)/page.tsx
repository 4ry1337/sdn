import { TopologyViewer } from "@/widgets/topology_viewer";

export default async function TopographyPage() {
  // 'use cache'
  // const data = await fetch('')

  return (
    <TopologyViewer />
  )
}
